import 'COMMON_UI_STYLE/global/global.js';
import './common.scss';

// app中调试时引入debug辅助代码 (--env dev 时生效)
if (__DEV__) {
    // window.addEventListener('load', function () {
    //    require('dev/debug');
    // });
}
